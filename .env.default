# Defaults for scripts/xcb. Not secret (the .p8 key file is the actual secret) —
# safe to commit. Copy to .env and override there for local values git shouldn't see.
ASC_KEY_ID=VXZC9LX827
ASC_ISSUER_ID=4997db28-c8eb-43d6-8b69-4b0598e56e18
